{
  "name": "event-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i ./src/input.css -o ./css/tailwind.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i ./src/input.css -o ./css/tailwind.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.4.18"
  }
}
